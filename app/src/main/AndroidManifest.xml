<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.gti.equipo4.smartapp">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_weight"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_weight_round"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/CustomAppTheme">
        </activity>

        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.IntroActivity"
            android:theme="@style/InitialSetupTheme" />
        <activity android:name=".activities.PreferenciasActivity" />

        <activity
            android:name=".activities.ConfigScaleActivity"
            android:label="@string/title_activity_config_scale"
            android:theme="@style/AppTheme">
        </activity>

    </application>

</manifest>
